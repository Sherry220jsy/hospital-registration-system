<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sherry.mapper.ModelCategoryMapper">


    <insert id="insert">
        insert into model_category (model_id,category_id,category_start_time,category_end_time,count,category_name,is_deleted) values
       (#{modelId},#{categoryId},#{categoryStartTime},#{categoryEndTime},#{count},#{categoryName},#{isDeleted})
    </insert>

    <update id="deleteByModelId">
    update  model_category
    set is_deleted = 1
    where model_id=#{modelId}
    </update>
    <update id="update">
        update model_category
<set>
    <if test="categoryStartTime != null">category_start_time=#{categoryStartTime},</if>
    <if test="categoryEndTime != null">category_end_time=#{categoryEndTime},</if>
    <if test="categoryId != null">category_id=#{categoryId},</if>
    <if test="count != null">count=#{count},</if>
    <if test="categoryName != null">category_name=#{categoryName},</if>
</set>
where model_category_id=#{modelCategoryId}
    </update>

    <select id="getByModelId" resultType="com.sherry.entity.ModelCategory">
        select * from model_category where model_id=#{modelId} and is_deleted != 1
    </select>
</mapper>
